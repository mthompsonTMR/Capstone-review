// src/types/slide.ts

export type Slide = {
  _id: string;
  pmSerialNumber: string;
  caseId: string;
  runId: string;
  slideId: string;
  createdBy: string;
  processDate: string;
  markerName: string;
  active: boolean;
};
